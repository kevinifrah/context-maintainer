{
  "description": "Tells the agent when a project's docs/context/ has fallen behind its code, so stale context is noticed at the start of a session rather than trusted by accident.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\"",
            "timeout": 20,
            "statusMessage": "Checking project context freshness"
          }
        ]
      }
    ]
  }
}
